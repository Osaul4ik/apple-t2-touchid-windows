<Window x:Class="T2TouchId.SepVaultGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SEP Vault — one-time setup" Height="300" Width="460"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,12"
                   Text="Разовий імпорт: user.kb шифрується DPAPI (machine scope) і зберігається в %ProgramData%\T2TouchId\sep-vault.bin. T2SepBootstrap читає цей файл при кожному холодному старті." />

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="KeybagPathBox" IsReadOnly="True" VerticalContentAlignment="Center" Margin="0,0,8,0"/>
            <Button Grid.Column="1" Content="Обрати user.kb…" Click="OnBrowseKeybag" Padding="10,4"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Special bag id:" VerticalAlignment="Center" Width="120"/>
            <TextBox x:Name="SpecialBagBox" Width="100" Text="-501"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Пароль:" VerticalAlignment="Center" Width="120"/>
            <PasswordBox x:Name="PasswordBox" Width="220"/>
        </StackPanel>

        <TextBlock Grid.Row="4" Text="Той самий пароль піде і для unlock(handle), і для unlock(special bag)." FontStyle="Italic" Foreground="Gray" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="5" x:Name="StatusText" TextWrapping="Wrap" Foreground="DarkRed" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Зберегти" Padding="16,6" Click="OnSave"/>
        </StackPanel>
    </Grid>
</Window>
